import styled from "styled-components";
const AuthWrapper = () => {
  return <Wrapper>AuthWrapper</Wrapper>;
};

const Wrapper = styled.section`
  min-height: 100vh;
  display: grid;
  place-items: center;
`;
export default AuthWrapper;
